from setuptools import setup, find_packages

setup(
    name='CFDSimulator',
    version='0.0.1',
    packages=find_packages(include=['CFDSimulator']),
    install_requires=[
    ],
    description="CFDSimulator Repository"
)